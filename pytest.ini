; [tool:pytest] is only valid in setup.cfg; pytest silently ignores this whole file with that spelling.
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --strict-markers
    --strict-config
    --verbose
    --tb=short
    --cov=planetscale_discovery
    --cov-report=term-missing
    --cov-report=html:htmlcov
    --cov-report=xml
    --cov-fail-under=60
markers =
    unit: marks tests as unit tests (deselect with '-m "not unit"')
    integration: marks tests as integration tests (deselect with '-m "not integration"')
    aws: marks tests that require AWS credentials/services
    db: marks tests that require database connection
    slow: marks tests as slow (deselect with '-m "not slow"')
    workload: marks tests covering query workload capture (deselect with '-m "not workload"')
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning